import { NgModule } from "@angular/core";
import { RouterModule } from "@angular/router";

@NgModule({
    declarations: [],
    imports: [RouterModule.forRoot([])],
    exports: [AppRoutingModel]
})
export class AppRoutingModel {}